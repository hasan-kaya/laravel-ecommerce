# PostgreSQL Configuration for High Load (1000 concurrent users)

# Connection Settings
listen_addresses = '*'
port = 5432
data_directory = '/var/lib/postgresql/data'
hba_file = '/var/lib/postgresql/data/pg_hba.conf'
ident_file = '/var/lib/postgresql/data/pg_ident.conf'
max_connections = 500                  # Default: 100 → Increased for 1000 users
shared_buffers = 256MB                 # 25% of RAM
effective_cache_size = 1GB             # 75% of RAM
maintenance_work_mem = 64MB

# Memory
work_mem = 16MB                        # Per-operation memory
wal_buffers = 16MB

# Query Planning
random_page_cost = 1.1                 # SSD
effective_io_concurrency = 200

# Write Ahead Log
wal_level = replica
max_wal_senders = 10
wal_keep_size = 1GB

# Checkpoints
checkpoint_completion_target = 0.9
checkpoint_timeout = 15min
max_wal_size = 2GB
min_wal_size = 1GB

# Logging
logging_collector = off
log_destination = 'stderr'
log_statement = 'none'                 # 'all' for debugging
log_duration = off
log_min_duration_statement = 1000      # Log slow queries > 1s

# Performance
# shared_preload_libraries = 'pg_stat_statements'  # Disabled for simplicity
